using UnityEngine;
using System.Collections.Generic;

// Defines exit waypoints and their connections
public class ExitWaypoints : MonoBehaviour
{
    public List<Transform> connectedWaypoints;
}

// Placeholder for entry waypoints, potentially holding connections in the future
public class EntryWaypoints : MonoBehaviour
{
    public List<Transform> connectedWaypoints; // Empty for now, but ready for future logic
}
